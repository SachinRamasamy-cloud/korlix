page ApiTest route "/api-test":
  get users "http://localhost:4000/api/users"

  section .p-6
    h1 "API Test"

    if usersLoading:
      p "Loading..."

    if usersError:
      p "{usersError}"

    for user in users:
      div .p-4 .border:
        h2 user.name
        p user.email