{
  "name": "korlix-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Korlix frontend runtime",
  "main": "dist/korlix.runtime.js",
  "scripts": {
    "build": "tsc && node build.js",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "esbuild": "^0.20.0"
  }
}
