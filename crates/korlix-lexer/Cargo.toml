[package]
name = "korlix-lexer"
version = "0.1.0"
edition = "2021"

[dependencies]
korlix-core = { path = "../korlix-core" }
korlix-ast = { path = "../korlix-ast" }
serde = { version = "1", features = ["derive"] }
